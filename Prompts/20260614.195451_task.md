# User Prompt
```
instead of black font on whit ebackground, make a blue onwhite background.
Make the change of the entire app
```

# Checklist

- [x] Modify colors.xml to change black to deep blue (#0D47A1)
- [x] Modify dashboard_card_gradient.xml to use primary color instead of hardcoded black
- [x] Update vector drawables to use blue fill (#FF0D47A1) instead of black fill (#FF000000)
- [ ] Build the app to verify changes compile and layout loads correctly (skipped as command run was not approved)
