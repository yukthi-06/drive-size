In the about section, Add developer name "Shibu Thittayil Narayanan"
Also add Build time stamp.
Also utilize the git commit Id and tag from "app\build.gradle" and show them in the About section

***

# Walkthrough - Developer, Build, and Git Info Added to About Section

We have updated the About card inside Settings to display the developer's name, the automated build timestamp, and version control credentials (Git SHA and Git Tag) generated directly during build-time compilation.

## Changes Made

### 1. Settings Layout Update (`app/src/main/res/layout/fragment_settings.xml`)
- Added three new `TextView` elements under the version text to show:
  - Developer Name (`@id/text_developer`)
  - Build Timestamp (`@id/text_build_time`)
  - Git Version Info (`@id/text_git_info`)

### 2. Version and Metadata Binding (`app/src/main/java/com/drivesizeviewer/ui/settings/SettingsFragment.java`)
- Imported the project's generated `BuildConfig` class.
- Bound and populated the developer name statically: `"Developer: Shibu Thittayil Narayanan"`.
- Read and displayed the compilation time from `BuildConfig.BUILD_TIMESTAMP`.
- Read and displayed Git metadata values from `BuildConfig.GIT_SHA` and `BuildConfig.GIT_TAG` to output version credentials dynamically.
